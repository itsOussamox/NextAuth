export const NotLoggedRoutes = [
    "/login",
    "/register"
]

export const ApiAuthRoutes = "/api/auth";
export const DefaultLoggedRoute = "/dashboard";
export const DefaultNotLoggedRoute = "/login";
export const PublicRouted = "/";